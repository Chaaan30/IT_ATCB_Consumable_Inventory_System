@model IEnumerable<IT_ATCB_Consumable_Inventory_System.Models.Item>

@{
    ViewData["Title"] = "Items";
}

<h2>Items</h2>

@* <form method="get" asp-action="Index">
    <input type="text" name="search" value="@Request.Query["search"]" placeholder="Search by model name..." class="form-control" />
    <button type="submit" class="btn btn-primary my-2">Search</button>
    <a href="@Url.Action("Create")" class="btn btn-success my-2">Add New Item</a>
</form> *@

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Model Name</th>
            <th>Category</th>
            <th>Supplier</th>       
            <th>Quantity</th>
            <th>Unit</th>
            <th>Critical Level</th>
            <th>Last Updated</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ModelName</td>
                <td>@item.Category?.Name</td>
                <td>@item.Supplier?.Name</td>
                <td>@item.CurrentQuantity</td>
                <td>@item.UnitOfMeasure</td>
                <td>@item.CriticalLevel</td>
                <td>@item.LastUpdated.ToString("yyyy-MM-dd")</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Details</a>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger" onclick="return confirm('Delete this item?');">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>